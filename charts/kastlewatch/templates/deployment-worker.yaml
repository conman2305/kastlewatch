apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kastlewatch.fullname" . }}-worker
  labels:
    {{- include "kastlewatch.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "kastlewatch.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "kastlewatch.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: {{ include "kastlewatch.serviceAccountName" . }}
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}/{{ .Values.image.name }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/app/kastlewatch", "worker"]
          workingDir: /config
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "kastlewatch.fullname" . }}-config
